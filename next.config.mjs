/** @type {import('next').NextConfig} */


const nextConfig = {
    i18n: {
        locales: ['vn', 'en'],
        defaultLocale: "vn",
        localeDetection: false,
    }
};
export default nextConfig;